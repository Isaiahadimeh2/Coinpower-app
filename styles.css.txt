/* Coinpower Master Stylesheet */
:root {
    --primary-gold: #ffcc00;
    --dark-bg: #121212;
    --card-bg: #1e1e1e;
    --text-main: #ffffff;
    --text-dim: #b3b3b3;
}

body {
    background-color: var(--dark-bg);
    color: var(--text-main);
    font-family: 'Inter', sans-serif;
    margin: 0;
    padding: 0;
}

/* Navigation Bar */
.navbar {
    display: flex;
    justify-content: space-between;
    padding: 15px 20px;
    background: var(--card-bg);
    border-bottom: 1px solid #333;
}

/* Miner Cards (PG2 - Solar Array) */
.miner-card {
    background: var(--card-bg);
    border-radius: 12px;
    padding: 20px;
    margin: 15px;
    border: 1px solid #333;
}

.miner-name {
    color: var(--primary-gold);
    font-weight: bold;
    font-size: 1.2rem;
}

/* Progress Bar (The 65% bar) */
.progress-container {
    width: 100%;
    background: #333;
    border-radius: 10px;
    height: 10px;
    margin: 10px 0;
}

.progress-bar {
    height: 100%;
    background: var(--primary-gold);
    border-radius: 10px;
    transition: width 0.5s ease;
}

/* Withdrawal Section & Buttons */
.btn-primary {
    background-color: var(--primary-gold);
    color: black;
    border: none;
    padding: 12px 24px;
    border-radius: 8px;
    font-weight: bold;
    cursor: pointer;
}

.btn-primary:hover {
    background-color: #e6b800;
}