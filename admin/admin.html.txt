<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>CoinPower | MASTER ADMIN</title>
    <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
    <style>
        :root { --gold: #D4AF37; --green: #28a745; --red: #dc3545; --dark: #121212; --light-dark: #1e1e1e; --white: #FFFFFF; }
        body { font-family: 'Segoe UI', sans-serif; background: var(--dark); color: var(--white); margin: 0; padding: 0; display: flex; height: 100vh; overflow: hidden; }
        
        /* Side Navigation */
        .sidebar { width: 250px; background: #0a0a0a; padding: 20px; display: flex; flex-direction: column; border-right: 1px solid #333; }
        .sidebar h2 { color: var(--gold); text-align: center; margin-bottom: 30px; }
        .nav-btn { background: none; border: none; color: #aaa; padding: 12px; text-align: left; cursor: pointer; font-size: 16px; width: 100%; border-radius: 8px; margin-bottom: 5px; }
        .nav-btn:hover, .nav-btn.active { background: #222; color: var(--white); }
        .nav-btn.active { border-left: 4px solid var(--gold); }
        
        /* Main Content Area */
        .main-content { flex: 1; padding: 30px; overflow-y: auto; }
        .page { display: none; }
        .page.active { display: block; }
        
        /* Common Elements */
        .card { background: var(--light-dark); padding: 20px; border-radius: 12px; border: 1px solid #333; margin-bottom: 20px; }
        h1, h3 { color: var(--gold); margin-top: 0; }
        table { width: 100%; border-collapse: collapse; background: #181818; border-radius: 8px; overflow: hidden; margin-top: 15px; }
        th, td { text-align: left; padding: 12px; border-bottom: 1px solid #333; }
        th { background: #2a2a2a; color: #aaa; font-weight: 600; }
        
        /* Buttons */
        .btn { padding: 8px 15px; border: none; border-radius: 6px; cursor: pointer; font-weight: bold; }
        .btn-green { background: var(--green); color: white; }
        .btn-red { background: var(--red); color: white; }
        .btn-gold { background: var(--gold); color: #1a1a1a; }
        
        /* Forms */
        input, select, textarea { width: 100%; padding: 12px; margin-bottom: 15px; background: #2a2a2a; border: 1px solid #444; border-radius: 8px; color: white; box-sizing: border-box; }
        
        /* Upload Room */
        .file-upload-box { border: 2px dashed #555; padding: 40px; text-align: center; border-radius: 12px; cursor: pointer; color: #888; margin-bottom: 20px; }
        .file-upload-box:hover { border-color: var(--gold); color: var(--white); }
        
        /* Modal for Generator Preview */
        .modal { display: none; position: fixed; z-index: 1000; left: 0; top: 0; width: 100%; height: 100%; background-color: rgba(0,0,0,0.8); }
        .modal-content { background: var(--light-dark); margin: 10% auto; padding: 20px; border: 1px solid var(--gold); width: 80%; max-width: 500px; border-radius: 12px; }
        .close { color: #aaa; float: right; font-size: 28px; font-weight: bold; cursor: pointer; }
        .close:hover { color: var(--white); }
        
    </style>
</head>
<body>

    <div class="sidebar">
        <h2>COINPOWER ADMIN</h2>
        <button class="nav-btn active" onclick="showPage('dashboard')">🏠 Dashboard</button>
        <button class="nav-btn" onclick="showPage('payments')">💸 Payments</button>
        <button class="nav-btn" onclick="showPage('users')">👤 Users</button>
        <button class="nav-btn" onclick="showPage('referrals')">🔗 Referrals</button>
        <button class="nav-btn" onclick="showPage('generators')">⚡ Generators</button>
        <button class="nav-btn" onclick="showPage('activities')">📜 Activities</button>
        <button class="nav-btn" onclick="showPage('uploads')">📷 Upload Room</button>
    </div>

    <div class="main-content">
        
        <div id="dashboard" class="page active">
            <h1>Admin Dashboard</h1>
            <p>Welcome to the CoinPower control center. Use the sidebar to navigate.</p>
        </div>

        <div id="payments" class="page">
            <h1>Payments Approval</h1>
            <p>Approve or reject deposits and withdrawals.</p>
            </div>

        <div id="users" class="page">
            <h1>Manage Users</h1>
            <div class="card">
                <h3>Account Search / Manage</h3>
                <input type="text" placeholder="Search by phone number...">
                <button class="btn btn-gold">Search</button>
            </div>
            <div class="card">
                <h3>Actions for Selected User</h3>
                <p>Selected: <strong id="selected-user-phone">None</strong></p>
                <div style="display: flex; gap: 10px;">
                    <button class="btn btn-red">Restart Account</button>
                    <button class="btn btn-gold">Add Bonus Balance</button>
                    <input type="number" placeholder="Bonus Amount ($)" style="width: 150px; margin: 0;">
                </div>
            </div>
        </div>

        <div id="referrals" class="page">
            <h1>Referral System</h1>
            <div class="card">
                <h3>Referral Logs</h3>
                <table>
                    <thead>
                        <tr><th>Referrer</th><th>Referred User</th><th>Reward</th><th>Date</th></tr>
                    </thead>
                    <tbody></tbody>
                </table>
            </div>
        </div>

        <div id="generators" class="page">
            <h1>Generator Management</h1>
            <div class="card">
                <h3>Create New Generator</h3>
                <div style="display: flex; gap: 10px;">
                    <input type="text" id="gen-name" placeholder="Generator Name (e.g. PG5)">
                    <button class="btn btn-gold" onclick="generateGenId()">Regenerate ID</button>
                </div>
                <input type="number" id="gen-price" placeholder="Price ($)">
                <input type="number" id="gen-income" placeholder="Income/Day ($)">
                <input type="number" id="gen-days" placeholder="Duration (Days)">
                <textarea id="gen-desc" placeholder="Description..."></textarea>
                
                <p style="margin-top: 15px;">Assigned Image ID: <strong id="gen-image-id">None</strong></p>
                <p style="font-size: 14px; color: #888;">Go to the Upload Room to assign an image for this ID.</p>
                
                <div style="display: flex; gap: 10px; margin-top: 20px;">
                    <button class="btn btn-gold" onclick="saveGenerator()">SAVE GENERATOR</button>
                    <button class="btn btn-green" onclick="publishGenerator()">PUBLISH TO MARKET</button>
                </div>
            </div>
            <div class="card">
                <h3>Current Generators Market</h3>
                <table>
                    <thead>
                        <tr><th>Name</th><th>Price</th><th>Income/Day</th><th>Status</th><th>Actions</th></tr>
                    </thead>
                    <tbody></tbody>
                </table>
            </div>
        </div>

        <div id="activities" class="page">
            <h1>Activities Room</h1>
            <div class="card">
                <h3>System Logs / Activity</h3>
                <p>A log of all actions on the platform.</p>
                <table>
                    <thead>
                        <tr><th>Timestamp</th><th>Action</th><th>Details</th></tr>
                    </thead>
                    <tbody></tbody>
                </table>
            </div>
        </div>

        <div id="uploads" class="page">
            <h1>Upload Room</h1>
            <div class="card">
                <h3>Upload Image for Generator</h3>
                <div class="file-upload-box">
                    <p>Drag & Drop or Click to Upload Image</p>
                    <small>Support for PNG, JPG (Max 5MB)</small>
                </div>
                <input type="text" id="upload-gen-id" placeholder="Enter Generator ID for this Image (e.g. PG1)">
                <button class="btn btn-gold">Assign Image to ID</button>
            </div>
        </div>

    </div>

    <script>
        // Supabase Initialization (Add your keys)
        const supabase = supabase.createClient('YOUR_URL', 'YOUR_KEY');

        // Page Navigation
        function showPage(pageId) {
            const pages = document.querySelectorAll('.page');
            const navBtns = document.querySelectorAll('.nav-btn');
            
            pages.forEach(p => p.classList.remove('active'));
            navBtns.forEach(b => b.classList.remove('active'));
            
            document.getElementById(pageId).classList.add('active');
            event.target.classList.add('active');
        }
        
        // Generator logic
        function generateGenId() {
            // Logic to create a new unique ID like 'PG5'
            document.getElementById('gen-image-id').innerText = "PG5"; // Placeholder
        }
        
        function saveGenerator() {
            // Logic to save draft to Supabase 'generators_draft' table
            alert("Draft Saved. You can publish from the market list.");
        }
        
        function publishGenerator() {
            // Logic to move draft to live 'generators_market' table and regenerate ID
            alert("Published to Market! Creating new ID...");
            generateGenId(); // Restart ID creation
        }
    </script>
</body>
</html>
